server:
  port: 8092
  servlet:
    context-path: /api/public
spring:
  application:
    name: route
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
        service: route
